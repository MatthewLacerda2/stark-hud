fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
mcp==2.1.1
# Reads the title, artist and album out of a music file. Pure Python, no system
# libraries behind it, and one dictionary over ID3, Vorbis comments and MP4.
mutagen==1.48.1
# The board's voice. The vendor's own SDK rather than a hand-rolled client: it
# carries the error bodies that tell a good key missing a scope apart from a bad
# one, which is the whole of the difference between two identical 401s.
elevenlabs==2.65.0
# Driving the board from a typed instruction, through Gemini's function calling.
# The vendor's own SDK rather than raw HTTP for one concrete reason: its
# `FunctionDeclaration.parameters_json_schema` takes a JSON Schema as written,
# so the fifty tool schemas the MCP server already publishes go across
# untouched. The `parameters` field beside it takes only Gemini's OpenAPI
# subset, which cannot express the `anyOf: [X, null]` every optional argument
# in this codebase serialises as.
google-genai==2.23.0
