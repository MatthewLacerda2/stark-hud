"""What the board costs, measured the same way twice.

`python -m perf` from `tools/`, or `make perf` from the repository root. See
`__main__.py` for what it does and `report.py` for what it compares against.

Deliberately not a gate. It needs a browser, and for a GPU figure it needs the
television; `make check` must run on a machine with neither.
"""
